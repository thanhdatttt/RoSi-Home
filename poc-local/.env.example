# Local-only PoC configuration. Never use these defaults in production.
POC_API_HOST=0.0.0.0
POC_API_PORT=3100
POC_DATA_DIR=../../.data/rosihome-poc
POC_JWT_SECRET=local-poc-development-secret-change-me
